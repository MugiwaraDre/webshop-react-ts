export type CardType = {
  title: string;
  price?: number;
  description: string;
  imgUrl: string;
};

export type user = {
  username: string;
  password: string;
  address: string;
  name: string;
};
